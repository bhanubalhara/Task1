# Database connection string (Neon Postgres)
DATABASE_URL=postgresql://user:password@host:5432/database

# Base URL for generating short links (optional, defaults to current origin)
# BASE_URL=http://localhost:3000
